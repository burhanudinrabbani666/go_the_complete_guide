# Anonymous Functions

```go
	transformed := TransformedNumbers(&numbers, func(number int) int { //<-- This Function Should be look like parameter 
		return number * 2
	})
```

Next: [Understanding closures](./04-understanding-closures.md)