# Switch statements

```go
		switch userChoice {

		case 1:
			fmt.Printf("Your Balance: %f\n", accountBalance)

    default:
      fmt.printLn("Thanks")
      return
    }

    // Break in switch if nor break for iterations. But for force close the app
```
Next: [Writing files](./37-writing-files.md)