# Creating formatted strings

```go
	formartedFV := fmt.Sprintf("Future value: %.2f \n", futureValue)
	formartedRFV := fmt.Sprintf("Future value (adjusted for inflation):  %.2f \n", futureRealValue)

	fmt.Print(formartedFV)
	fmt.Print(formartedRFV)
```

Next: [Building multiline strings](./22-building-multiline-strings.md)